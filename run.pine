#!/bin/sh
cn="\033[1;32;40m"
cf="\033[0m"
printc(){
	echo -e "${cn}$@${cf}"
}
## prepare
printc "preparing..."
./prepare.pine

## init the repo
printc "initializing the repo..."
./repo.pine

## build the tree 
printc "building the tree..."
./make.pine

## build image
printc "building image..."
./build.pine
